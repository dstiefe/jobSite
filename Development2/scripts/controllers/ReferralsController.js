/**
 * Created by Van on 23.02.2016.
 */

angular.module('Jobsite').controller('ReferralsController', function ($scope, JobsService, ScreeningsService, $sce, $timeout, $document) {

    //$scope.resume = resume;
    //
    //JobsService.getJob(jobId).then(function (results) {
    //    response = results.data;
    //    $scope.job = response;
    //}, function (error) {
    //    console.log(error.data.message);
    //});
    //
    //
    //ScreeningsService.getTestResultsByResumeId($scope.resume.id).then(function (results) {
    //    response = results.data;
    //    $scope.results = response;
    //}, function (error) {
    //    console.log(error.data.message);
    //});
    //$scope.onClose = function() {
    //    $modalInstance.close();
    //}

});
